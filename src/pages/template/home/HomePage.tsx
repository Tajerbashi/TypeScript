import ReactContent from "../../../components/HOC/ReactContent";

const HomePage = () => {
  return (
    <ReactContent>
      <div>Home Page</div>
    </ReactContent>
  );
};

export default HomePage;
